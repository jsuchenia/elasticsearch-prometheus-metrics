# Integration tests for JVM Example Plugin
#
"elasticsearch-prometheus-metrics loaded":
    - do:
        cluster.state: {}

    # Get master node id
    - set: { master_node: master }

    - do:
        nodes.info: {}

    - match:  { nodes.$master.plugins.0.name: elasticsearch-prometheus-metrics }
